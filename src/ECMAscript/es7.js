// feature released in 2016.

/* Array method includes */

const values = [233,455,12];
const string = 'you need to find if i contain -rec-';

if(values.includes(value => value === 455)) {
	console.log('value included');
} 
if(!values.includes(value => value === 13)) {
	console.log('value not included');
}

console.log(string.includes('rec'));

/*  Exponent */
let base = 4;
let exponent = 3;
const result = base ** exponent;
console.log(result);